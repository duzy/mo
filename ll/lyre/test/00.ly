decl a_boolean = true;
decl a_integer = 1;
decl a_float = 0.1;
decl a_string = "foobar";

#say("%d", a_boolean);
#say("string: %s", a_string);
#say("string: %f", a_float);
say("string: %d", a_integer);

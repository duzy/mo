decl a_boolean = true;
decl a_integer = 0;
decl a_float = 0.1;
decl a_string = "foobar";

say("boolean: %d", a_boolean);
say("integer: %d", a_integer);
say("float: %f", a_float);
say("string: %s", a_string);

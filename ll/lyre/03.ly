# proc fun(a) --- 2 * 2; ---

a = (1 + fun(2) + 3) * 3;

a = b = c = 1 + 2 + 3;

#v = a.foo() + a.bar() * 3 + a.foobar + a[0] + a{ .name == "foo" } + .value;

v = a.foo() + a.bar() * 3 + a.foobar;

.name;
.name.foo();
->name;
->name.name.foo();
